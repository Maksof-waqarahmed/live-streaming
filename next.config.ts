import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ["https://rana-classroom-app.loca.lt", "http://localhost:3000"]
};

export default nextConfig;
